import React from "react";
import { Alert, Box } from "@chakra-ui/react";

export default function TopVisitedItems() {
    return (
        <Box>
            <Alert status="error">There was an error processing your request</Alert>
        </Box>
    );
}
